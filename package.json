{
  "name": "aspiring-futures-gala",
  "version": "1.0.0",
  "private": true,
  "description": "Live gala donation display. This manifest only holds the dev/test tooling (unit tests via node --test, end-to-end via Playwright); the app itself is a build-free static site with no runtime dependencies.",
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
