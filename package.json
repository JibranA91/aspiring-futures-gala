{
  "name": "aspiring-futures-gala",
  "version": "1.0.0",
  "private": true,
  "description": "Live gala donation display. This manifest exists only to run the core-logic tests; the app itself is a build-free static site with no dependencies.",
  "scripts": {
    "test": "node --test"
  }
}
